--Install all dependencies

npm install

--Create a .env file in the root folder.
--Copy all text from env_sample and paste it into .env.

--Start the app

npm run dev

--Open the app in your browser

http://localhost:5173/

Login info

First, log in as a normal user.

Then, go to the Superbus database and update your role to admin if needed.
(Otherwise, you can continue as a user or create your own database.)